{
  "name": "vector-search",
  "private": true,
  "packageManager": "bun@1.2.23",
  "scripts": {
    "dev": "concurrently -k -n api,ui -c blue,green \"uv run --directory backend uvicorn backend.main:app --reload --host 127.0.0.1 --port 8010\" \"cd frontend && bun run dev\"",
    "dev:api": "uv run --directory backend uvicorn backend.main:app --reload --host 127.0.0.1 --port 8010",
    "dev:ui": "cd frontend && bun run dev",
    "build": "cd frontend && bun run build",
    "check": "cd frontend && bun run check"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
